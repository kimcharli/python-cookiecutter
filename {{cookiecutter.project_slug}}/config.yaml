# {{ cookiecutter.project_name }} global configuration
# All paths are relative to the repository root unless absolute.
#
# Environment variable prefix: {{ cookiecutter.pkg_name | upper }}_
# Point to a custom config file: {{ cookiecutter.pkg_name | upper }}_CONFIG=/path/to/config.yaml
#
# Uncomment and edit any line to override the compiled-in default.
# Run `{{ cookiecutter.project_slug }} --show-config` to see all effective values and their sources.

# input_dir:  data/input
# output_dir: data/output
# logs_dir:   data/logs
# log_level:  INFO
# web_port:   8000
